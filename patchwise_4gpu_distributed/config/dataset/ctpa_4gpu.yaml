name: FULL_RESOLUTION_CTPA_4GPU
root_dir: /workspace/Xray-to-CTPA/datasets/rsna_drrs_and_nifti/
image_channels: 1

# Dataset limiting (use null for all data, or set number for subset)
max_patients: 500  # Train on subset (7,279 total) - set to null for all data

# Patch configuration
patch_size: [128, 128, 128]  # D, H, W
stride: [128, 128, 128]      # Non-overlapping patches

# Train/val split
train_split: 0.8

# Data preprocessing
normalize: true
augmentation: false
