# Full Resolution CTPA Dataset Configuration
# Supports 512×512×604 volumes with patch extraction

name: FULL_RESOLUTION_CTPA
root_dir: /workspace/Xray-to-CTPA/datasets/
image_channels: 1

# Patch extraction parameters  
patch_size: [128, 128, 64]   # Reduced patch size to fit memory (H, W, D)
stride: [128, 128, 64]        # No overlap - just tile the volume

# Data split
train_split: 0.8

# Preprocessing
normalize: true
augmentation: false  # Can add augmentation later

# Statistics
# Full volume: 512×512×604
# Patch size: 256×256×128
# Stride: 192×192×96
# Patches per volume: ~24 (3×3×3 grid)
# Overlap: 25% per dimension (64 voxels)
